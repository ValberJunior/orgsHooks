import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        marginHorizontal: 16,
        marginTop: 16
    },
    title:{
        fontSize: 20,
        lineHeight: 32,
        fontWeight: 'bold',
        color: '#464646'
    }
});

export default styles;